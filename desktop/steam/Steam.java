package org.mslivo.core.sandtrixplus.steam;
    /*
public class Steam {


    private static boolean initialized = false;
    private static boolean steamAPIAvailable = false;

    private static boolean isSteamAPIAvailable() {
        if (!CFG_MAIN.STEAMAPI_ENABLED) return false;
        if (!initialized) {
            try {
                SteamAPI.loadLibraries();
                steamAPIAvailable = SteamAPI.init();
            } catch (Exception e) {
                Tools.logError(e);
                steamAPIAvailable = false;
            }
        }
        return steamAPIAvailable;
    }

    public static void setStarAchievement(int stars) {
        if (!isSteamAPIAvailable()) return;
        SteamUserStats steamUserStats = new SteamUserStats(null);
        steamUserStats.setAchievement("sssddf");

    }

    public static void shutDown(){
        if(initialized){
            SteamAPI.shutdown();
        }
    }

}
     */
